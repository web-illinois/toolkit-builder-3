<!DOCTYPE html>
<html dir="ltr" lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
      <!-- Preconnect -->
    <link rel="dns-prefetch" href="//cdn.toolkit.illinois.edu">
    <link rel="dns-prefetch" href="//cdn.brand.illinois.edu">
    <link rel="dns-prefetch" href="//cdn.disability.illinois.edu">
    <link rel="dns-prefetch" href="//onetrust.techservices.illinois.edu">

      <!-- Illinois Toolkit -->
    <link rel="icon" href="https://cdn.brand.illinois.edu/favicon.ico">
    <link rel="stylesheet" href="https://cdn.brand.illinois.edu/illinois.css">
    <link rel="stylesheet" href="{{ environment.baseurl }}toolkit.css">
    <script type="module" src="{{ environment.baseurl }}toolkit.js"></script>
    <title>{{ title }}</title>
    <style>
      ilw-page-title img {
        -webkit-mask-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0));
        width: 100%;
      }
    </style>
  </head>
  <body style="margin: 0;">
    <ilw-page class="ilw-margin ilw-font">
      {% render 'header' %}
      <main>
        {%- if title_background %}
          <ilw-page-title
            width="page"
            {% if title_background_color %}
            style="background: {{ title_background_color }}"
            {% endif %}>
            <img
              src="{{ title_background }}"
              alt=""
              slot="background">
            <h1>{{ title }} - {{ environment.title }}</h1>
          </ilw-page-title>
          <ilw-spacer></ilw-spacer>
        {%- else %}
          <ilw-content>
            <h1>{{ title }} - {{ environment.title }}</h1>
          </ilw-content>
        {%- endif %}
        {%- if lede %}
          <ilw-content mode="lede">
            <p>{{ lede }}</p>
          </ilw-content>
        {% endif %}
        {{ content }}
      </main>
      {% render 'footer' %}
    </ilw-page>
  </body>
</html>